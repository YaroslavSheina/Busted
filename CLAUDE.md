# BUSTED (рабочее название; прежнее — Left / Right) — мобильная игра про угонщика

Игрок — угонщик, выполняющий заказы: получить машину, провести через трафик, доставить в точку.
Вид строго сверху. Машина едет сама. Управление — только две кнопки: LEFT и RIGHT.
Полный концепт-документ лежит в `docs/concept.md` — читай его при любых вопросах о дизайне.
Расширенный диздок «BUSTED» (хаос, ущерб, очки, читы, монетизация) — `docs/design.md`;
решения по нему — в разделе «Решения по диздоку BUSTED» ниже, они главнее текста диздока.
Референс ощущения управления — `docs/prototype.html`. Это эталон: после переноса игра должна
ощущаться идентично ему. Не «улучшай» физику при переносе.

## Зафиксированные решения (не обсуждаются без явного запроса)

1. **Камера следует за машиной, но никогда не поворачивается.** Ориентация экрана постоянна.
   Центр камеры — точка впереди машины по вектору её скорости (не курса), с плавным догоном.
2. **LEFT и RIGHT — относительно машины, не экрана.** Когда машина едет вниз, LEFT смещает её вправо по экрану.
   Это главная механика, а не баг. Никаких подсказок, инвертирующих или «помогающих» управление.
3. **Газа и тормоза нет.** Скорость постоянна и задаётся машиной (уровнем).
4. **Инерция — угловая.** Кнопки не двигают машину вбок и не переключают полосы; они добавляют угловую скорость.
   Боковое смещение — следствие курса. После отпускания кнопки курс не возвращается к дороге сам.
5. **Одна ошибка — конец попытки.** Столкновение или серьёзный вылет за дорогу → `BUSTED` → мгновенный рестарт.
   Экран поражения обязан объяснять причину в терминах ввода: «держал LEFT 0.8 с».
   Экран хаоса (см. ниже) этого не отменяет: один тап в любой момент перематывает счётчик до итога и перезапускает.
6. **Трафик детерминирован.** Одинаковый seed на уровень: каждая попытка идентична, проигрыш — вина игрока, не рандома.
7. **Машины — данные, не код.** Новая машина = новая запись в таблице параметров, никаких новых классов.
8. **Уровни — данные.** Контрольные точки сплайна + ширина + машина + плотность трафика, в JSON.
9. **Игрок никогда не получает новых кнопок.** Разнообразие = машина × скорость × дорога × трафик.

## Решения по диздоку BUSTED (`docs/design.md`)

Принято 2026-09-06. Текст диздока — источник идей; при расхождении действует этот список.

0. **Два режима, ядро — первое.** (2026-09-07) Игра делится на «угнать и довезти» (мастерство: дорога, трафик,
   посты, погоня, очки) и «круши-кромсай» (шоу: хаос, ущерб, события). Сейчас делаем только первое.
   Хаос — отдельный режим, отложен целиком: фаза C и всё, что от неё зависит (D-паника-в-хаосе, F события,
   G читы как источники ущерба), не начинаются без отдельного решения. Фиксированный шаг времени — тоже отложен,
   он нужен хаосу и рейтингу по ущербу, а не ядру.

1. **Физика остаётся угловой.** Диздок пишет «смещается влево-вправо», но нужные ему инерция и микро-заносы
   даёт именно наша модель. Формула не меняется, машины различаются только числами в `cars.ts`.
2. **Хаос пропускается одним тапом.** Экран хаоса (Crash Mode) — шоу после `BUSTED`, а не задержка рестарта.
   Тап в любой момент: счётчик ущерба мгновенно доматывается до итога, попытка перезапускается.
3. **Масштаб хаоса зависит от заезда.** Чем дальше проехал и чем больше Near Miss, тем больше копов тянется за игроком
   и тем больше куча. Ущерб — награда за вождение, а не лотерея. Разбиться на старте должно быть невыгодно.
4. **Ущерб — валюта прогресса, рейтинг — только детерминированные очки.** В шкалу открытия машин идёт весь ущерб,
   включая случайные события и читы. В глобальный рейтинг идут очки вождения (дистанция, Near Miss, множитель финиша),
   либо ущерб — но только если симуляция хаоса детерминирована (фиксированный шаг, seed уровня, никакого `Math.random`).
   Заезд с читом рейтинг не получает (`cheat = true` на сессию).
5. **Детерминизм — цель.** Заезд должен описываться seed уровня + логом нажатий: это даёт честный рейтинг,
   воспроизводимые баги и «поделиться аварией» в сотню байт. Переход игры на фиксированный шаг времени
   меняет интегрирование физики — делать только после явного согласия и проверки ощущения.
6. **Хаос сначала на коробках.** Crash Mode собирается и проверяется на телефоне на прямоугольниках до любого арта.
   Если куча-мала из серых коробок не смешит, рисунок её не спасёт.
7. **События хаоса и объекты уровня — данные.** Бензовоз, автобус, поезд, вертолёт, брёвна, шуточный финал —
   записи в таблице событий с шансом и условием (нужен переезд / ЛЭП). ЛЭП, переезды, здания — слой объектов
   в JSON уровня и в редакторе, не код.
8. **Чит-коды — последовательности тапов на отсчёте 3-2-1.** Не добавляют кнопок. Первый и единственный
   до проверки — «без ремня».
9. **Копы — несколько преследователей с простым ИИ.** Текущий `chaser` расширяется до списка; копы накапливаются
   с дистанцией, паникующий трафик может их снести. Никакого «умного» объезда трафика копами.
10. **Near Miss — расширенный OBB.** Проверка близости через тот же SAT с увеличенным габаритом; сработавшая
    провокация паники — с шансом, из seeded rng уровня.
11. **«Воскреснуть за рекламу» — не более одного раза за заезд**, только у финиша; это монетизация,
    а не механика, и на баланс уровней не влияет.
12. **Рендер остаётся Canvas 2D**, пока измерение на среднем Android в фазе хаоса не докажет обратное.
    `render.ts` — единственное место, знающее про canvas, чтобы переход на WebGL не трогал игру.

## Физическая модель (из прототипа, воспроизводить точно)

Система координат: canvas, y вниз. Курс `h` в радианах, `h = 0` — вверх по экрану, рост `h` — поворот по часовой (вправо).
Вектор вперёд `fwd = (sin h, −cos h)`. Вектор вправо `right = (cos h, sin h)`.

Состояние машины: позиция `(x, y)`, курс `h`, угловая скорость `w`, вектор скорости `(vx, vy)`, флаг `skid`.

На каждом кадре с `dt` (зажат сверху 1/30):

```
dir     = (RIGHT ? 1 : 0) − (LEFT ? 1 : 0)          // −1, 0, +1
sensMul = 1 + (speed / 300 − 1) · sens               // чувствительность растёт со скоростью

w += dir · steer · sensMul · dt                      // кнопка = угловое ускорение
w −= w · (skid ? damp · 0.25 : damp) · dt            // гашение вращения; курс НЕ выравнивается
if (!skid && |w| > spin)        skid = true          // сорвало
if ( skid && |w| < spin · 0.4)  skid = false         // поймал

h += w · dt
g  = min(1, (skid ? skidGrip : grip) · dt)           // как быстро вектор скорости догоняет курс
vx += (fwd.x · speed − vx) · g
vy += (fwd.y · speed − vy) · g
x  += vx · dt ;  y += vy · dt
```

Почему так: гашение `damp` даёт «несёт после отпускания» (курс продолжает меняться ~`w/damp` радиан),
отсутствие возврата курса даёт «надо компенсировать другой кнопкой», порог `spin` и падение сцепления в
заносе дают «закрутило, но можно поймать контррулением». Скорость усиливает всё через `sensMul`.

Стартовые значения (проверены в прототипе на 300 px/с):

| параметр | значение | смысл |
|---|---|---|
| `speed` | 300 | px/с, задаётся машиной |
| `steer` | 5 | угловое ускорение от кнопки, рад/с² |
| `damp` | 3 | гашение вращения, 1/с |
| `grip` | 7 | сцепление, 1/с |
| `spin` | 2.4 | порог заноса по |w|, рад/с |
| `skidGrip` | 1.2 | сцепление в заносе |
| `sens` | 1 | вклад скорости в чувствительность (0 — не зависит) |

Параметры машины (таблица `cars`): `speed, steer, damp, grip, spin, skidGrip`. Всё остальное общее.

## Дорога, трафик, столкновения

- Дорога — Catmull-Rom сплайн по контрольным точкам, отсэмплированный в точки каждые ~6 px с накопленной длиной `s`,
  касательной `(tx, ty)` и правой нормалью `n = (−ty, tx)`.
- Положение машины на дороге: ближайшая точка сэмпла в окне вокруг предыдущего `s` (не глобальный поиск),
  боковое смещение `off = dot(pos − p, n)`.
- Вылет: `|off| > width/2 + tolerance` → BUSTED. `tolerance` — балансовая ручка прощения.
- Полосы: до `LANES = 3`, число полос по местной ширине `clamp(floor(width / LANE_MIN), 1, 3)` (≥ 135 — три, ≥ 90 — две),
  смещение полосы `i` из `n` полос = `(i − (n − 1)/2) · width / n`. При ширине 150 это ровно `(i − 1) · width / 3` прототипа.
- Трафик: каждая машина — `{ s, lane, speed }`, движется вдоль сплайна в своей полосе, ориентация = касательная.
  Скорость 40–70 % от игрока. Если впереди в той же полосе машина ближе 110 px — подстраивается под неё.
- Столкновения: OBB против OBB, SAT по четырём осям. Проверять только трафик в окне `|s − car.s| < 120`.
- Финиш: `car.s ≥ L − 60` → DELIVERED.

## Стек

- Vite + TypeScript + Canvas 2D. **Без игрового движка** — физика 40 строк, движок только прячет её.
- Ввод: pointer events с `setPointerCapture` на двух зонах внизу экрана; обе зоны могут быть зажаты одновременно.
  Клавиатура: стрелки / A, D. `touch-action: none`, без скролла и зума.
- Позже: Capacitor для iOS/Android. До этого тестируем по ссылке GitHub Pages; с экрана «Домой» игра открывается на весь экран
  в портрете (`public/manifest.webmanifest`, иконки из ключевого арта в `public/icons`, мета-теги iOS в `index.html`).
- Сохранение — localStorage, ключи `lr.*`: `campaign`, `progress`, `fame` (campaign.ts), `car` (shell.ts), `sound` (audio.ts),
  `haptics` (haptics.ts), `tester` (tester.ts), `log` (log.ts). Каждое обращение в try/catch: приватный режим, харнесс в Node.
  Версии сохранения нет: переименование ключа уровня в кампании отбрасывает игрока, стоящего на нём, к прологу.

## Структура

```
src/
  main.ts          — страница игры: собирает UI, createGame, панель тюнинга
  game.ts          — одна попытка: createGame(ui, level) — состояние, update, цикл, resize, DPR
  config.ts        — все тюнинг-параметры в одном месте (см. таблицу выше)
  physics.ts       — модель машины, чистые функции: step(car, input, params, dt)
  road.ts          — сплайн, сэмплирование, pathAt(s), nearest(pos, sGuess); касательная по окну ≥3 px без разворотов — микропетли равномерного Катмулла-Рома не переворачивают нормаль
  traffic.ts       — спавн (seeded), движение, OBB, SAT
  input.ts         — зоны LEFT/RIGHT, клавиатура, трекинг длительности удержания
  render.ts        — камера, drawGrid/drawRoad (общие с редактором), точка А и гараж Б, машины, следы заноса, HUD; тема pixelnight основная (ночная улица по референсам docs/refs), pixel — день, pixeldusk — закат (тема уровня из JSON, ?theme= сильнее), night/comic/dark/bright/sprites — прежние и эксперименты; статика рисуется только в видимой области (setClip/visible), точки дороги через STEP
  audio.ts         — звук: синтез Web Audio (мотор, сирена, одиночные эффекты по именам), контекст после первого жеста; в Node заглушка
  icons.ts         — временные пиксельные иконки 16×16 (ASCII → SVG, цвета из токенов style.css)
  shell.ts         — карточка района, гараж, пауза, настройки (экраны вне заезда); haptics.ts — вибрация
  map.ts           — карта карьеры как город: холст движком уровней (районы своими палитрами), точки уровней и таблички DOM, карточка уровня, гараж
  tester.ts        — режим тестера: все уровни на карте открыты (?tester=1 или пять тапов по номеру сборки в настройках)
  log.ts           — журнал теста (lr.log): каждая попытка (итог, место, время, кадры/с), запуски игры, заметки тестера; отправка файлом
  env.d.ts         — константы сборки: __BUILD__ — хэш коммита и дата (vite.config.ts, define), на загрузочном экране и в настройках
  art.ts           — растровый арт: спрайты машин, предметов (конус, ежи, коробки, погрузчик, контейнер) и тайлы из public/art (корень — Higgsfield, pixel/ — референсы), паттерны, подкраска, процедурные тайлы
docs/ui.md         — интерфейс как система: стиль, принципы для телефона, экраны, список графики, порядок
docs/feedback.md   — что добавлено и ждёт фидбека с телефона (ведётся с 2026-09-14)
docs/career.md     — карьера: ручки сложности, кривая-пила, районы, концептуальные уровни, решения
docs/ideas.md      — бэклог идей (2026-09-17): режимы, цели уровней, город, динамические преграды, типы погонь; всё со статусом «идея»
docs/refs/         — референсы визуала от пользователя (машины и предметы сверху, образцы улицы днём и ночью, взрывы, ключевой арт hero-image)
  levels.ts        — загрузка levels/*.json через import.meta.glob, ключ = имя файла
  cars.ts          — таблица машин: name, body (рисунок), W/L, speed, steer, damp, grip, spin, skidGrip
  blocks.ts        — заграждения: данные уровня → геометрия (посты, ежи, ремонт, обочины)
  crossings.ts     — перекрёсток: поперечная улица, группа машин по циклу, светофор (crossCars, lightAt)
  rails.ts         — переезд: рельсы через дорогу, детерминированный поезд (trainObb, untilTrain)
  rings.ts         — кольцо (M11): полный круг, остров и чужие рукава — окружение для рисования и причины BUSTED; маршрут по кругу строит генератор
  narrow.ts        — сужения: ширина как функция от s (makeWidthFn/widthAt), переходы NARROW.ramp
  roads.ts         — граф дорог: ветка { from, to, points, parent? } пристыковывается к родителю (главная или другая ветка), buildRoadPaths, parentEquivalent для прогресса
  debug.ts         — панель «уровень»: список уровней + кнопка «тюнинг» со слайдерами (в проде слайдеры выключены)
  campaign.ts      — порядок уровней кампании и локальный прогресс (localStorage); сценарий уровня — docs/progression.md
  editor/
    main.ts        — панель, экспорт/импорт, «Играть» через createGame на той же странице
    canvas.ts      — холст: панорама, зум, точки; дорога рисуется drawRoad
    io.ts          — формат файла уровня, валидация импорта
levels/*.json      — { name, points, width, traffic, seed, car?, cars?, chaser?, blocks?, branches?, panic?, narrows?, rails?, crossings?, props?, oncoming?, mix?, pace?, grip?, theme?, rings?, intro?, cards?, trap?, checkpoints? } — см. docs/mechanics.md и docs/progression.md
tools/district.cjs — генератор районов: район (сетка, здания, машина) × маршруты (дороги по узлам: главная + ветки с родителем, перекрёстки, события: посты, ремонт, перекрытия, сужения, переезды) → levels/district*.json, один район — несколько уровней
tools/harness/     — харнесс в репозитории (2026-09-17): bundle.mjs склеивает dist (vite build --minify false) в port.mjs с хуком __lr; compare.mjs — покадровое сравнение с прототипом («порт идентичен прототипу»); routebot.mjs — бот прохождения уровней (LEVEL, LANE, PLAN, CLEAN, KEEPCOP, KEEPRAMP, OVERTAKE, LOOK, TAKE, TRACE); sweep.mjs — бот по всей кампании (список из campaign.ts) по планам полос plans.json, без трафика («все уровни кампании проходимы»). `npm run harness` = сборка + сравнение + боты. port.mjs не в git. После правок game/physics/traffic/road/main и уровней — харнесс обязателен; новый уровень кампании — план в plans.json
.github/workflows/ci.yml — на каждый пуш и pull request: типы и харнесс; выкладка на Pages только из main после зелёной проверки
README.md          — запуск, флаги адреса, харнесс, порядок работы вдвоём (для коллег и GitHub)
docs/concept.md
docs/design.md     — диздок BUSTED
docs/mechanics.md  — лаборатория механик: карточки идей со статусами, формат данных каждой
docs/prototype.html
docs/prototype-roads/ — пять дорог прототипа (эталон для покадрового сравнения, в игру не грузятся)
editor/index.html  — редактор уровней (вторая страница Vite, /editor/)
```

Уровень задаёт `width/traffic` и машину `car` (ключ из `cars.ts`, по умолчанию `sedan`); при загрузке
числа машины и уровня пишутся в `P`, слайдеры панели тюнинга крутят их поверх. `seed` — детерминированный трафик уровня. `cars` — явно расставленные машины (speed px/с, 0 — стоит),
добавляются к seeded-трафику; при `traffic: 0` остаются только они.
`chaser` — полиция на хвосте: стартует на `gap` px позади и едет по сплайну со скоростью `speed` × скорость игрока,
повторяя его боковое смещение (`CHASER_FOLLOW` в config). В повороте радиуса R коп «едет по линии» на `CHASER_LINE` px
снаружи осевой, то есть теряет ход в `(1 − CHASER_LINE/R)` раз, как приличный водитель; на прямой не теряет.
Правило: на прямой догнать нельзя, в поворотах догоняет того, кто едет шире линии или делает лишние манёвры.
Откалибровано ботом (scratchpad `chase.mjs`): приличный бот не под угрозой, небрежный пойман. Касание = BUSTED «догнали».

`physics.ts` не знает про canvas, DOM и дорогу. `road.ts` не знает про машину. Это позволяет тестировать физику числами.

## Порядок работ

Каждая фаза заканчивается проверкой на телефоне и коммитом. Фазы A–C — до любых разговоров об арте.

- [x] **0. Перенос.** `prototype.html` разложен по модулям, ощущение идентично (проверено покадровым сравнением).
- [x] **0. Редактор уровней.** `/editor/`: точки, машины, преследователь, экспорт/импорт, «Играть». `polygon.json` — одна трасса
  со всеми типами участков для проверки ощущения (прямая, 15°…135°, разворот, змейка на инверсии, кольцо). Прежние уровни 01–03
  и тестовые уровни механик удалены 2026-09-24 — в `levels/` только кампания и полигон.
- [x] **A. Ощущение.** Таблица машин `cars.ts` (минивэн, седан, масл-кар, спорт, спорткар пролога и финала, автобус), машина задаётся
  уровнем. Обучение пересобрано в городе генератором (docs/progression.md); меню «авто» и «уровень» — только `?debug=1`.
- [x] **B. Очки.** Near Miss, впритирку к посту, провокация, снос копа, дистанция, множитель финиша ×5, счёт в HUD,
  искры и всплывающие надписи. Комбо — после теста. См. docs/mechanics.md M2.
- [ ] **C. Хаос v1 — ОТЛОЖЕНО (решение 0: отдельный режим «круши-кромсай»).** Состояние `chaos` после столкновения: тела с массой и вращением, простой отклик на удар,
  копы и трафик влетают в кучу, взрыв по порогу удара, счётчик $ по таблице цен, отдаление камеры,
  пропуск одним тапом. Здесь же решение по фиксированному шагу и seed (решения 4–5). На коробках.
- [~] **D. Копы и провокация.** Паника трафика, слоу-мо и зум, копа сносит паникёр — сделано (M3).
  Осталось: список копов и накопление с дистанцией — если ядру это нужно, а не хаосу.
- [x] **E. Прогресс.** Вместо «ущерб → шкала» (хаос отложен): кампания по ключу уровня, слава, три звезды, гараж с ярусами машин,
  экран результата, отсчёт 3-2-1 — docs/progression.md, docs/ui.md. Сохранение — localStorage, ключи `lr.*` (раздел «Стек»).
- [ ] **F. События хаоса — ОТЛОЖЕНО вместе с C.** Формат события как данных, слой объектов уровня в редакторе (ЛЭП, переезд, здания).
  Сначала бензовоз и шуточный финал, остальные по одному.
- [ ] **G. Чит-коды — ОТЛОЖЕНО (эффекты живут в хаосе).** Матчер последовательностей на отсчёте, флаг сессии, «без ремня» (ragdoll водителя).
- [~] **H. Арт и звук.** Пиксельная графика по референсам пользователя (тема pixelnight, docs/mechanics.md «Визуал»), взрывы и дым,
  звук синтезом (audio.ts). Осталось: рисованные иконки и логотип (docs/ui.md, шаг 6), музыка, семпл мотора, если синтез не зайдёт.
- [~] **MVP-тест** (2026-09-24). Сделано: пауза при сворачивании, карточки один раз за загрузку уровня, строка про нажатие
  на BUSTED, первый запуск сразу в пролог, установка на экран «Домой», номер сборки, режим тестера, заметка тестера, журнал теста
  с отправкой файлом, тепловая карта смертей в редакторе, проверки на GitHub (харнесс и боты по кампании, выкладка только после
  зелёной проверки). Дальше: раунд теста на 5–8 человек (docs/feedback.md), решения пользователя — масштаб камеры (сколько дороги
  видно впереди зависит от телефона и направления), инверсия (езда вниз по экрану), объём MVP, сервис для журнала вместо файла.
- [ ] **I. Релиз.** Capacitor, английский, политика конфиденциальности, возрастной рейтинг, монетизация (Rewarded Video, решение 11),
  бэкенд рейтинга с флагом читов (нужен фиксированный шаг, решение 5), магазины. Отдельный проект.

Кривая сложности — docs/career.md. Концепт (docs/concept.md) задумывал главной ручкой поворот дороги: вертикаль → 30° → 45° →
90° → развороты → кольца. В городе углы 90° с первого уровня, а езда вниз по экрану (полная инверсия) есть только в прологе,
первом уровне обучения и финале — решение по ней после теста.

## Правила для Claude Code

- Перед изменением физики — спросить. Числа в `config.ts` крутить можно, формулу — нет.
- Не добавлять механики, кнопок, ускорений, тормозов, «помощи в управлении».
- Каждое изменение должно быть проверяемо на телефоне: после задачи — `npm run dev` с доступом по сети (`--host`)
  или ссылка GitHub Pages после пуша (номер сборки — на загрузочном экране).
- Перед пушем — `npm run harness`: сравнение с прототипом и боты по кампании. GitHub проверяет то же и без зелёной проверки
  не выкладывает. Новый уровень кампании — план полос в `tools/harness/plans.json`.
- Имена верхнего уровня в модулях уникальны на весь бандл: харнесс склеивает чанки сборки в один файл, и одинаковые
  `const`/`let` из разных чанков ломают склейку (так было со STEP и cache).
- Код короткий и читаемый; никаких абстракций «на будущее». Класс появляется, когда есть второй экземпляр.
- Комментарии — только о том, почему, не о том, что.
- Язык общения — русский, идентификаторы в коде — английские.
- Работа идёт итерациями через Git: закончил осмысленный кусок (модуль, фича, фикс) — проверил — коммит.
  Один коммит = одно изменение, сообщение по-русски: что и зачем. Не копить несвязанные правки в одном коммите.
